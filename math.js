const raio = 5.6;
/*Dessa forma se consegue pegar os valores de PI como valor diretamente da função. Também se consegue calcular valor em potencia com
* a função pow
*/
const area = Math.PI *Math.pow(raio,2);

console.log(area);

//Nesse caso indica que a funcao MATH é um objeto.
console.log(typeof Math);